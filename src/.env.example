# Copy this file to `.env` (same directory: src/.env) and fill in your keys.
# Never commit the real .env — it is gitignored.

# Required: content generation (gpt-4o) + interview + CEO judgment
OPENAI_API_KEY=

# Required: quality judge (Groq llama-3.3-70b)
GROQ_API_KEY=

# Required: competition analysis web search (Serper, free 2,500/mo)
SERPER_API_KEY=

# Optional: fallback provider (Gemini)
GEMINI_API_KEY=
