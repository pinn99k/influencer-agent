# 세션 34 리뷰 — MVP 하드닝: 측정 레이어 + 견고성 마감
date: 2026-06-11
참여: 핑구(회장) + Claude(개발자)

## 한 줄 요약
로드맵 1순위였던 측정·실험 레이어(provenance 포함)를 TDD로 구현하고,
풀 E2E 드라이런에서 찾은 이음새 버그와 견고성 공백 4건을 전부 마감했다.

## 진행 흐름
1. 계획 재수립: 세션 31 로드맵(M->D->T)과 대조 — 측정이 1순위인데 미착수임을
   확인, 이전 하드닝 계획의 순서를 교정.
2. 풀 E2E 드라이런(실 LLM): CEO.run() 폴백 0건(Track0 V-1 양호). 이음새 버그
   1건 — 검증기 4글자 반복체크가 '스타일링' x35를 복붙으로 오판 -> 윈도우 8/임계
   10으로 실측 보정.
3. P1 측정 레이어: measure.py(KPI/변수태깅/의사결정 provenance) + AI/사람 자동
   후킹 + API + 매니저 도크 입력 UI. 브라우저로 저장->집계 검증.
4. P2 견고성: 테스트 위생 fixture / 판단 폴백 7곳 emit / 채팅 jsonl 영속+복원 /
   재분석 입력 로딩 단일화. 캐시버스팅(부팅토큰)으로 stale js 혼란 제거.

## 잘된 점
- 로드맵 권위를 계획에 복원(측정 우선) — 4주 운영 데이터가 노이즈가 될 뻔한
  것을 사전 차단.
- 모든 기능 TDD(red->green) + 브라우저 검증 + 실측 기반 임계 보정.
- provenance(AI/사람) 강제 — 포폴 진정성 리스크 대응을 코드 레벨로.

## 아쉬운 점 / 미해결
- 핑구 직접 웹 E2E 미실행(다음 단계).
- 실제 미용사 정보 수집(임계경로, 사람 병목) 미착수.
- T(최소 자율 루프) 미착수 — 로드맵대로 운영 Week1 병행 예정.
- 측정 UI는 입력만 — 주차별 비교 시각화는 데이터 쌓인 뒤.

## 다음 세션 시작점
1. 핑구 웹 E2E(인터뷰->분석->채팅->방향->재분석->측정 입력) 피드백 수정.
2. 컨시어지 온보딩: 실제 미용사 정보로 1차 분석 -> 전략요약+주간카드 전달.
3. 운영 시작 후 T(최소 자율 루프) 착수.

## 산출물
- 코드: core/measure.py, api/routes/measure.py, 채팅 영속(fm/chat_engine/chat
  라우트/bootRecover), ceo.py(_emit_fallback 7곳, _load_reanalyze_inputs,
  AI 결정 로깅), validators(_NGRAM_WINDOW 8), main.py(캐시버스팅), 측정 UI.
- 테스트: +30 (343 pass). 문서: ADR 030, status.md, 본 리뷰.
