# 세션 38 리뷰 — 2장 자율루프 제품 연결
- 날짜: 2026-06-16
- 범위: CEO.run_autonomous를 CLI(--autonomous) + 웹(mode)에 연결 + 프론트 토글 + 테스트.

## 한 일
1. CLI: main.py --autonomous 플래그 + (autonomous and not dry_run) 분기.
2. 웹: JobContext.mode + start_job(mode=) + _run_ceo 분기 + job_completed에 mode.
   /start, /interview/confirm 라우트가 mode 수용·검증·전달.
3. 프론트: 인터뷰 확정 바 '자율 모드' 토글(State.runMode) -> interviewConfirm mode 전달.
   확정 바 세로 스택 레이아웃(좁은 컬럼 글자 압착 해결).
4. 테스트 위생: main.py win32 stdout 재래핑을 pytest 하에서 스킵(캡처 깨짐 방지).
   기존 stub 2개(test_interview_api/direction) 시그니처에 mode 추가.

## 검증
- 379 pass(373->379, 신규 test_autonomous_wiring 6). 한자 0. node --check 4 OK. api.main import OK.
- 브라우저 스모크(상태 주입, LLM 비용 없음): 토글 렌더/클릭/재렌더/콘솔0/레이아웃 정렬.

## 남은 것
- 자율 모드 실 LLM 풀 E2E는 비용 발생 -> 핑구가 웹 토글로 직접 1회 확인 권장.
- E단계(워크플로우 vs 에이전트 A/B 비교)는 이제 실 시스템 경로로 셋업 가능.
- CLAUDE.md/README 명령 예시에 --autonomous 추가는 후속(선택).

## 결정
- ADR 034. 선형 run() 기본·폴백 유지, 자율은 옵트인.


## 후속 (같은 세션 — 자체 코드리뷰 + T0/T1)
1. 자체 코드리뷰(실행 없이): 호출/응답 흐름 트레이싱.
   - 결함 F1(수정): run_autonomous가 run()과 달리 _load_direction/성과 주입을 건너뜀
     -> 자율 모드에서 사용자 방향 누락. ceo.py run_autonomous에 동일 주입 추가.
   - 무회귀로 확인된 의심들: job_completed mode(완료메시지 'reanalyze'만 분기),
     대시보드 /plan(04_컨셉기획+최종리포트만 읽음 -> 자율도 생성), 데모 start(mode 기본 linear).
   - 경미(미수정): runMode 끈적임(T0에서 처리), 토글 시 textarea 휘발(기존 패턴),
     --autonomous+--reanalyze 우선순위(설계상 OK).
2. T0: State.runMode reset 초기화 + run_autonomous 방향주입 회귀 가드 테스트.
3. T1 자율 루프 관찰성: ToolExecutor provenance 로깅(MeasureStore AI 결정) +
   AgentLoop stop_reason + loop_stopped emit. 신규 테스트 7(direction1+observability6).
4. 검증: 386 pass(373->386), 한자 0, node --check OK. 실 LLM e2e 미실행(다음 세션).
