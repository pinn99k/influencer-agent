# ADR 035 — MVP 마감: 웹 복구 버그 + 견고성 + 최종리포트 실제 성과 반영 + 공개 레포
date: 2026-06-21 (세션 39)
status: accepted

## 배경
실 LLM e2e 점검에서 사용성 결함 발견. "MVP 끝, 확장 다음" 결정에 따라 마감 + 공개 레포 준비.

## 결정
1. 웹 상태 누수 2건(핑구 발견):
   - MD_CACHE 교차오염: 산출물 캐시를 파일명만으로 키잉 -> 대상자명+파일명, clearMdCache 무효화.
   - 매니저 알림 디스크 복원: file_manager.load_manager_outputs + GET /api/manager/{name}
     + actions.restoreManagerNotes (openSubject/bootRecover).
2. 견고성: llm_client 3개 함수가 ConnectionError/Timeout 백오프 재시도(429/503만 처리하던 갭).
3. 출력 정확성:
   - [추정] 누출 가드(촬영 장비, 성공 지표).
   - _synthesize_final_report 페이로드에 성과_기록 주입 + final_report 프롬프트가 실제 달성치 반영.
4. 테스트 격리: test_call_llm_tools/test_ceo_decide_next에 더미 키 -> 키 없는 환경(공개 클론)에서도 green.

## 검증 (실 gpt-4o)
- 자율 1회: 방향/provenance(decision_log)/stop_reason 정상.
- 피드백 재분석: 컨셉기획 타게팅 + 촬영 팁 반영.
- 4주 성과 시뮬(주차별 누적 재분석 4회): W1 보수적 -> W2 전면개편(일상 제거/비포애프터+저녁)
  -> W3 수렴(재실행 없음) -> W4 갱신. 지능적 적응.
- 최종리포트 직접 합성: 성공지표가 실제 달성치(팔로워 330/저장수 180/예약문의) 반영, [추정] 제거.
- 392 pass(386->392, 신규 manager_api 4 + minimal_fixes 2). 한자 0.

## 공개 레포
- src/outputs 전체 삭제.
- 클론(D:/House/influencer-agent) src 동기화(.env/outputs/캐시 제외) + README 392 + 키 독립 테스트.
- 클론 키 없이 391 pass + 1 skip(live). 커밋 63888af. push 대기(원격 URL + 핑구 확인).

## 한계(비차단)
- _decide_rerun 수렴 시 최종리포트 재생성 스킵 -> 디스크 반영은 다음 실재실행 때. Fix 자체는 검증됨.
- 페르소나 칩 잔상(cosmetic).
